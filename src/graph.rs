use ndarray

#[derive(Debug)]
pub struct Graph {
    pub nodes: Vec<Node>,
}
impl Graph {
    // Constructor
    pub fn new(horizontal_size: int, vertical_size: int) -> Game {
        Graph {
            nodes: type Array2<A> = Array<A, Ix2>;
        }        
    }
    
    //Methods
    pub fn get_neighbours(&mut self, x: int, y: int) {
        
    }

}